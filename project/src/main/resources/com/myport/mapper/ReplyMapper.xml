<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.myport.mapper.ReplyMapper">
  		<insert id="enrollReply">
  			insert into test_reply(bookId, memberId, content, rating) values(#{bookId}, #{memberId}, #{content}, #{rating})
  		</insert>
  		
<!--   		<select id="read" resultType="com.myport.domain.ReplyVO">
  			select * from tbl_reply
  			where rno=#{rno}
  		</select>
  		
  		<delete id="delete">
  			delete from tbl_reply where rno=#{rno}
  		</delete>
  		
  		<update id="update">
  			update tbl_reply set reply=#{reply}, updatedate=sysdate
  			where rno=#{rno}
  		</update>
  		
  		<select id="getListWithPaging" resultType="com.myport.domain.ReplyVO">
  			select rno, bno, reply, replyer, replydate, updatedate
  			from <![CDATA[
  			(select /*+INDEX(tbl-reply pk_reply) */
  			rownum rn, rno, bno, reply, replyer, replyDate, updatedate
  			from tbl_reply
  			where bno=#{bno}
  			and rno>0
  			and rownum <= #{cri.pageNum} * #{cri.amount})
  			where rn >(#{cri.pageNum}-1) * #{cri.amount}
  			]]>
  		</select>
  		
  		<select id="getCountByBno" resultType="int">
  			select count(rno) from tbl_reply where bno=#{bno}
  		</select>
  		
  		<delete id="deleteAll">
  			delete from tbl_reply where bno=#{bno}
  		</delete> -->
  </mapper>